name: Test
on:
  push:
  pull_request:
jobs:
  test:
    runs-on: self-hosted
    name: Test
    steps:
    - uses: actions/checkout@v3
    - uses: asdf-vm/actions/install@v4
    - run: go test ./... -timeout 120s
